export const KEY_SETTINGS_UI_LANG = 'vsw.settings.ui.language';
export const KEY_SETTINGS_UI_MENU_ORDER = 'vsw.settings.ui.menu.order';

export const KEY_SVGE_IMAGE_SIZE = 'vsw.svgexporter.image.size';

export const KEY_BASE64_IMAGE_DECODER_IMAGE_SIZE =
    'vsw.base64image.decoder.image.size';
export const KEY_BASE64_IMAGE_DECODER_IMAGE_TYPE =
    'vsw.base64image.decoder.image.type';
export const KEY_BASE64_IMAGE_ENCODER_IMAGE_SIZE =
    'vsw.base64image.encoder.image.size';

export const KEY_COLOR_PICKER_FAVORITES = 'vsw.colorpicker.favorites';
export const KEY_COLOR_PICKER_IMAGE_SIZE = 'vsw.colorpicker.image.size';

export const KEY_FILECHECKSUM_HASH_TYPE = 'vsw.filechecksum.hash.type';

export const KEY_SQLFORMATTER_SQL_LANGUAGE =
    'vsw.sqlformatter.sql.language';

export const KEY_SVGO_IMAGE_SIZE = 'vsw.svgoptimizer.image.size';

export const KEY_TIMESTAMP_CONVERTER_DATETIME_FORMAT =
    'vsw.timestampconverter.datetime.format';

export const KEY_GAMES_TYPING_FONT_SIZE = 'vsw.games.typing.font.size';
export const KEY_GAMES_TYPING_DARK_MODE = 'vsw.games.typing.dark.mode';
